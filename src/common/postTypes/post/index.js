import getStatusManager from "./status";
import getConstants from "./constants";
import setContents, {requestForUserPage} from "./contents";

const postTypeConfig = {
    getStatusManager,
    getConstants,
    setContents,
    requestForUserPage
};

export default postTypeConfig;
