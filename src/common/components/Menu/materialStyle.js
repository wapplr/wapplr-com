export default function makeMaterialStyle(theme) {
    return {
        listItem: {
            color: theme.palette.text.primary
        },
        listItemIcon: {
            color: theme.palette.text.primary
        },
    }
}
